/**
 * @author chen
 * @description API 接口出口 自动导出modules下的接口
 * @update 2022-07-03 15:05:00
 */

// const requireAPI = import.meta.globEager('./modules/*.ts')
// const API: any = {}

// Object.entries(requireAPI).forEach(([k, v]) => {
//   Object.assign(API, v)
// })

// export default API

// API 选择按需导入 这里就不做自动导出
